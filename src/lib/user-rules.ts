/** Username rules shared by the staff-account server actions and their forms (client-safe). */
export const USERNAME_RULE = /^[a-z0-9_.-]{3,32}$/;
export const USERNAME_HINT = "3–32 characters: lower-case letters, numbers, dot, dash or underscore.";
